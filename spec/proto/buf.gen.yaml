# Generates spec/gen/ from the spec protos and the conformance harness protos.
# Run `bundle exec rake proto:generate`.
version: v2
plugins:
  - remote: buf.build/protocolbuffers/ruby:v35.1
    out: ../gen
